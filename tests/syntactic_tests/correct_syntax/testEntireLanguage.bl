var int hello; #these two lines test all the possibilits for the list of variables declared
var int goodbye [10];
var int newNum [10];
var int newerNum;

procedure int proc1( int argOne, int argTwo ) { #test next proc to have no args, also this is testing listinstr
  proc1( argOne, argTwo );
  input ( goodbye [10] );
  output ( 66  ) ;
  while (proc1(6, 10) < 4) {
    int idTwo [40]; #decl
    int idThree; #other decl case
  }
  if ((6 <= 10) || (5 >= 2) || (3 == 4) || (true && false) || (true || false) || (! ((goodbye [10] + ( 6 - 4)) > 4))) {
    newNum [ 66 ] := 6 % 3;
    newerNum := (8 + (3 * (2 / 1)));
    return 5;
  } else {
    if ( 10 > 5 ) {
      return 4;
    }
    endif
  }
  endif
}

procedure int main() {
  int array [2];
}





